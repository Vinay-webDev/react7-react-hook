/* react hook = A special function that allows functional
                components to use React features without 
                writing class components (Reat V16.8)
                (useState(), useEffect(), useContext(), 
                useReducer(), useCallback(), and more...)

useState() = A React hook that allows to create a stateful
             variable AND a setter function to update it's
             value in the virtual DOM */
import MyComponent from './MyComponent.jsx'

function App() {
  

  return (
    <>
      <MyComponent/>
    </>
  )
}

export default App
